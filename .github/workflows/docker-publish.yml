# This workflow is deprecated - use ci-cd.yml instead
# This file is kept for historical reference and will be removed in the future

name: Legacy Docker Build (DEPRECATED)

on:
  workflow_dispatch:  # Only manual trigger

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/dooray-mcp-legacy

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecation Notice
      run: |
        echo "⚠️ This workflow is deprecated!"
        echo "Please use the new ci-cd.yml workflow instead."
        echo "This legacy workflow will be removed in a future version."