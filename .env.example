# Dooray MCP Server Configuration

# Required: Dooray API credentials
DOORAY_API_KEY=your_api_key_here
DOORAY_BASE_URL=https://api.dooray.com

# Optional: Logging configuration
DOORAY_LOG_LEVEL=WARN          # DEBUG, INFO, WARN, ERROR (default: WARN)
DOORAY_HTTP_LOG_LEVEL=WARN     # HTTP client logging level (default: WARN)

# Optional: Server configuration (HTTP mode only)
# These can also be passed as command line arguments
# DEFAULT_HOST=127.0.0.1
# DEFAULT_PORT=8080
# DEFAULT_PATH=/mcp